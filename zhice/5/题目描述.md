# 第五题：实现一个自定义的 HashMap（简化版）

## 题目描述
实现一个简化版的 HashMap，支持核心功能：

- **put(K key, V value)**：添加/更新键值对，处理哈希冲突（链地址法）
- **get(K key)**：根据 key 获取 value，不存在则返回 null
- **remove(K key)**：根据 key 删除键值对
- **支持自动扩容**（负载因子 0.75，容量翻倍）

## 要求
- 使用链地址法处理哈希冲突
- 负载因子超过 0.75 时自动扩容
- 扩容时容量翻倍，并重新哈希所有元素
