# 第四题：实现一个线程安全的阻塞队列

## 题目描述
基于 ReentrantLock 和 Condition 实现一个有界阻塞队列，支持以下核心功能：

- **put(E e)**：当队列满时，调用线程阻塞；队列有空位时，添加元素并唤醒等待的消费者。
- **take()**：当队列空时，调用线程阻塞；队列有元素时，取出元素并唤醒等待的生产者。
- **支持获取队列大小、判断空/满等辅助方法**。

## 要求
- 使用 ReentrantLock 保证线程安全
- 使用 Condition 实现精确的线程等待/唤醒
- 支持多线程并发访问
