apiVersion: v1
kind: Namespace
metadata:
  name: {{K8S_NAMESPACE}}
  labels:
    name: {{K8S_NAMESPACE}}
    app.kubernetes.io/name: {{PROJECT_NAME}}
    app.kubernetes.io/version: "{{PROJECT_VERSION}}"
